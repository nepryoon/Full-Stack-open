```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a note and clicks "Save" (SPA)

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa (JSON payload)
    activate server
    server-->>browser: 201 Created (or 200 OK) + JSON response
    deactivate server

    Note right of browser: Browser updates the UI locally (adds the new note) without reloading the page
